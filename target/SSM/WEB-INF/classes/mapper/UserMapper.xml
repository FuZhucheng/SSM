<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fuzhu.dao.UserDao">
    <!--<insert id="insertUser" parameterType="com.fuzhu.entity.User">-->
        <!--insert ignore  into USER (id,account,avatar,pay_money,country,create_time,score,latitude,longitude,province,sex,password)-->
        <!--VALUES (#{},#{},#{},#{},#{},#{},#{},#{},#{},#{},#{},#{})-->
    <!--</insert>-->
</mapper>